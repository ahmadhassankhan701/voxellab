<section class="section section--brand-story" data-section-id="{{ section.id }}">
  <div class="container">
    <div class="brand-story brand-story--centered">
      <div class="brand-story__content">
        <p class="brand-story__body">
          {%- if section.settings.body != blank -%}{{ section.settings.body }}{%- else -%}{{ 'home.about_body' | t }}{%- endif -%}
        </p>
        <a href="{{ section.settings.button_link | default: '/pages/about' }}" class="btn btn--inverse btn--pill">
          {{ 'home.discover_story' | t }}
        </a>
      </div>
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Brand story",
  "tag": "section",
  "class": "section-brand-story",
  "settings": [
    {
      "type": "textarea",
      "id": "body",
      "label": "Body text"
    },
    {
      "type": "url",
      "id": "button_link",
      "label": "Button link"
    }
  ],
  "presets": [
    {
      "name": "Brand story"
    }
  ]
}
{% endschema %}
