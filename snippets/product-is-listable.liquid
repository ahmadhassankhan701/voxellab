{%- liquid
  assign listable = true

  unless product.available
    assign listable = false
  endunless

  if product.tags contains 'test' or product.tags contains 'demo' or product.tags contains 'hidden'
    assign listable = false
  endif
-%}
{{- listable -}}
